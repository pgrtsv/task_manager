rosrust::rosmsg_include!(
    std_msgs / Bool,
    std_msgs / String,
    std_msgs / Empty,
    std_msgs / Float32,
    geometry_msgs / PointStamped,
    detection_msgs / DetectedObjects,
    task_manager / DroneStatus,
    task_manager / Start,
    visualization_msgs / Marker,
    std_srvs / Empty,
    std_srvs / SetBool,
    transform_services / TransformPoint,
    transform_services / TransformPose,
    pos_collector_msgs / NearestPos,
    pos_collector_msgs / GetAll,
    pos_collector_msgs / Count,
    fast_planner_server / FastPlannerAction,
    motion_controller / Spin,
    autotakeoff / Takeoff,
    nodes_monitor_msgs / Status,
    plan_env / AddWalls,
    sensor_msgs / BatteryState,
    qr_detector_msgs / QRCode,
    qr_detector_msgs / QRCodeArray,
    nav_msgs / Path,
    mavros_msgs / PositionTarget
);
rosrust_actionlib::action!(self; fast_planner_server: FastPlanner);
